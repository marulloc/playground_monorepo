'use client';

/**
 * @TODO 1. NextAuth - Cart
 * @TODO 2. Authenticated Cart Recoil
 * @TODO 3. useCart hook
 * @returns
 */
const useCart = () => {
  return [];
};

export default useCart;

/**
 *
 * 0. Image Gallery Carousel 로직을 Compound Pattern으로 만들어본다.
 * 0. Recommendation에도 적용해본다.
 * 0. Selected Variant의 이미지를 보이는 작은 UI를 만든다 (add to cart 위에)
 * 0. useSelectVariant Hook 만들자 useSyncDataUrl과 getSelected
 *
 * 1. NextAuth 로 인증 및 회원가입 로직 작성한다.
 * 2. 로그인 폼을 내 Form에서 사용한다.
 * 3. Authenticated Cart 로직 작성하고
 * 4. Cart state에 anonymous Cart와 authenticated Cart를 병합하는 global state를 작성한다.
 * 5. useCart hook을 만들고 addtocart, updateLine, deleteLine을 적용한다.
 * 6. Side Cart의 UI 형태를 컴파운드 컴포넌트로 작성한다.
 * 7. add to cart가 발생할때 toast가 나오는 걸 만든다.
 * 8. Navigation을 제작하고 드랍다운을 Compound Pattern으로 만들어본다.
 *
 *
 *
 */
